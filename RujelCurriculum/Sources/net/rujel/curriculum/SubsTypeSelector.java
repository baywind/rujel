package net.rujel.curriculum;

import net.rujel.interfaces.EduLesson;

import com.webobjects.appserver.*;

// Generated by the WOLips Templateengine Plug-in at Oct 2, 2009 2:04:12 PM
public class SubsTypeSelector extends com.webobjects.appserver.WOComponent {

	public WOComponent returnPage;
    public EduLesson lesson;
    
    public SubsTypeSelector(WOContext context) {
        super(context);
    }
    
    public void setSubstitute(Object sub) {
    	;
    }
    
    public WOActionResults addSubstitute() {
		WOComponent editor = pageWithName("EditSubstitute");
		editor.takeValueForKey(returnPage, "returnPage");
		editor.takeValueForKey(lesson, "lesson");	    	
		editor.takeValueForKey(null, "substitute");
		return editor;
    }
    
    public WOActionResults addJoinSub() {
		WOComponent editor = pageWithName("EditSubstitute");
		editor.takeValueForKey(returnPage, "returnPage");
		editor.takeValueForKey(lesson, "lesson");	    	
		editor.takeValueForKey(null, "substitute");
		editor.takeValueForKey(new Integer(1), "idx");
		return editor;
    }
    
    public WOActionResults addJoin() {
		WOComponent editor = pageWithName("EditJoin");
		editor.takeValueForKey(returnPage, "returnPage");
		editor.takeValueForKey(lesson, "lesson");	    	
		return editor;
    }
}