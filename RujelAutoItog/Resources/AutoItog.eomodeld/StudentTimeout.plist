{
    attributes = (
        {
            allowsNull = Y; 
            columnName = COURSE; 
            name = courseID; 
            prototypeName = integer; 
        }, 
        {columnName = "FIRE_DATE"; name = fireDate; prototypeName = date; }, 
        {allowsNull = N; columnName = FLAGS; name = flags; prototypeName = byte; }, 
        {allowsNull = N; columnName = ITOG; name = itogID; prototypeName = short; }, 
        {columnName = REASON; name = reason; prototypeName = string; }, 
        {
            allowsNull = N; 
            columnName = STUDENT; 
            name = studentID; 
            prototypeName = integer; 
        }, 
        {columnName = "T_ID"; name = tID; prototypeName = "int_3"; }
    ); 
    attributesUsedForLocking = (studentID, reason, flags, courseID, fireDate, itogID); 
    className = "net.rujel.autoitog.StudentTimeout"; 
    classProperties = (autoItog, fireDate, flags, reason); 
    externalName = TIMEOUT; 
    fetchSpecificationDictionary = {}; 
    name = StudentTimeout; 
    primaryKeyAttributes = (tID); 
    relationships = (
        {
            destination = AutoItog; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = aiID; sourceAttribute = itogID; }); 
            name = autoItog; 
        }
    ); 
}
