{
    attributes = (
        {
            allowsNull = N; 
            columnName = "IN_YEAR_COUNT"; 
            name = inYearCount; 
            prototypeName = byte; 
            valueType = i; 
        }, 
        {columnName = NAME; name = name; prototypeName = word; }, 
        {columnName = TITLE; name = title; prototypeName = anagram; }, 
        {columnName = "T_ID"; name = typeID; prototypeName = short; }
    ); 
    attributesUsedForLocking = (name, inYearCount, title); 
    className = "net.rujel.eduplan.PeriodType"; 
    classProperties = (inYearCount, name, templates, title); 
    externalName = "PERIOD_TYPE"; 
    fetchSpecificationDictionary = {}; 
    name = PeriodType; 
    primaryKeyAttributes = (typeID); 
    relationships = (
        {
            deleteRule = EODeleteRuleDeny; 
            destination = EduPeriod; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = perType; sourceAttribute = typeID; }); 
            name = periods; 
            ownsDestination = Y; 
        }, 
        {
            deleteRule = EODeleteRuleCascade; 
            destination = PeriodTemplate; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = perType; sourceAttribute = typeID; }); 
            name = templates; 
            ownsDestination = Y; 
            propagatesPrimaryKey = Y; 
        }
    ); 
}
